apiVersion: v1
kind: Service
metadata:
  name: spring-boot-starter-kit
  labels:
    app: spring-boot-starter-kit
spec:
  type: ClusterIP
  selector:
    app: spring-boot-starter-kit
  ports:
    - port: 8080
      targetPort: 8080
      name: http
